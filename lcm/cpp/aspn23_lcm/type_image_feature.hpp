/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen 1.5.2
 **/

/**
 * This code is generated via firehose.
 * DO NOT hand edit code.  Make any changes required using the firehose repo instead
 */
#ifndef __aspn23_lcm_type_image_feature_hpp__
#define __aspn23_lcm_type_image_feature_hpp__

#include <lcm/lcm_coretypes.h>

#include <vector>

namespace aspn23_lcm
{

/// Image feature characteristics
class type_image_feature
{
    public:
        /**
         * Non ASPN. Do not use. Extra field encoding the struct name to disambiguate LCM type fingerprint hashes.
         */
        int8_t     icd_type_image_feature;

        /**
         * Description: Value representing the magnitude of the response of the keypoint detection
         * algorithm used to locate this keypoint in the image.
         * Units: none
         */
        double     response;

        /**
         * Description: Value representing the orientation of the detected keypoint, as measured relative
         * to the image coordinate system (radians, clockwise). Metadata flag determines if this field is
         * calculated by the current keypoint detector.
         * Units: rad
         */
        double     orientation;

        /**
         * Description: Effective size (pixels) of the detected keypoint. This field takes into
         * consideration the octave and kernel used to localize the keypoint, and normalizes to the original
         * size of the image.
         * Units: pixels
         */
        double     size;

        /**
         * Description: ID that ties the detected keypoint to a class or group of keypoints.
         * Units: none
         */
        int32_t    class_id;

        /**
         * Description: Octave or pyramid layer from which the detected keypoint was extracted.
         * Units: none
         */
        int32_t    octave;

        /**
         * Description: Size of the descriptor in bytes.
         * Units: none
         */
        int32_t    descriptor_size;

        /**
         * Description: Feature descriptor.
         * Units: none
         * LCM Type: byte[descriptor_size]
         */
        std::vector< uint8_t > descriptor;

    public:
        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to read while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "type_image_feature"
         */
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static uint64_t _computeHash(const __lcm_hash_ptr *p);
};

int type_image_feature::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int type_image_feature::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int type_image_feature::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t type_image_feature::getHash()
{
    static int64_t hash = static_cast<int64_t>(_computeHash(NULL));
    return hash;
}

const char* type_image_feature::getTypeName()
{
    return "type_image_feature";
}

int type_image_feature::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->icd_type_image_feature, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->response, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->orientation, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->class_id, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->octave, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->descriptor_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    if(this->descriptor_size > 0) {
        tlen = __byte_encode_array(buf, offset + pos, maxlen - pos, &this->descriptor[0], this->descriptor_size);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int type_image_feature::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->icd_type_image_feature, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->response, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->orientation, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->class_id, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->octave, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->descriptor_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    if(this->descriptor_size) {
        this->descriptor.resize(this->descriptor_size);
        tlen = __byte_decode_array(buf, offset + pos, maxlen - pos, &this->descriptor[0], this->descriptor_size);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int type_image_feature::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __byte_encoded_array_size(NULL, this->descriptor_size);
    return enc_size;
}

uint64_t type_image_feature::_computeHash(const __lcm_hash_ptr *)
{
    uint64_t hash = 0xb7044cccc86ac036LL;
    return (hash<<1) + ((hash>>63)&1);
}

}

#endif
