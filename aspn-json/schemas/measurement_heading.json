{
  "$comment": "This file is generated via firehose. DO NOT edit by hand. Make any changes required using the firehose repo instead.",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/is4s/aspn-generated/tree/main/aspn-json/schemas/measurement_heading.json",
  "title": "measurementHeading",
  "version": 2023,
  "systemType": "standard",
  "category": "measurement",
  "controlled": false,
  "distribution": "A",
  "experimental": false,
  "description": "Local level, geographic attitude expressed in the locally level NED frame as a rotation about the D-axis.  Positive rotation follows the right-hand rule.  Attitude about the other axes in the NED-frame are unspecified and not sensed.  Use attitude_2d or attitude_3d to report a sensed dimension that is zero.  See also NED coordinate frame definition for N, E, and D definitions.",
  "type": "object",
  "properties": {
    "header": {
      "description": "header: Standard ASPN measurement header.",
      "$ref": "type_header.json"
    },
    "timeOfValidity": {
      "description": "time_of_validity: Time at which the measurement is considered to be valid.",
      "$ref": "type_timestamp.json"
    },
    "reference": {
      "description": "reference (enum): Defines heading reference.",
      "type": "integer",
      "oneOf": [
        {
          "const": 0,
          "description": "TRUE_HEADING: True heading.  Direction to the geographic north expressed in the locally level NED frame as a rotation about the D-axis.  This is akin to attitude_1d, which would be consistent with attitude_2d and attitude_3d. In that case, a ref frame enum could be used to define multiple one-dimensional attitudes, of which, heading could be one.  The simpler approach of just having heading was chosen over the more general approach until there is a requirement for multiple, one-dimensional attitudes."
        },
        {
          "const": 1,
          "description": "MAGNETIC_HEADING: Magnetic heading.  Direction to magnetic north expressed in the locally level NED frame as a rotation about the D-axis.  In general, geographic position and time is required to convert from magnetic heading to true heading."
        }
      ]
    },
    "obs": {
      "description": "obs: Heading.  Units: rad",
      "type": "number"
    },
    "variance": {
      "description": "variance: Variance of the measurement.  Units: rad^2",
      "type": "number"
    },
    "errorModel": {
      "description": "error_model (enum): Defines an optional error model for other than zero-mean, additive, white Gaussian noise (AWGN).",
      "type": "integer",
      "oneOf": [
        {
          "const": 0,
          "description": "NONE: No additional error model provided (num_error_model_params = 0)."
        }
      ]
    },
    "numErrorModelParams": {
      "description": "num_error_model_params: Number of parameters required for the error model chosen.",
      "type": "integer"
    },
    "errorModelParams": {
      "description": "error_model_params: Error model parameters that characterize the optional error model. Units: various",
      "type": "array",
      "items": {
        "type": "number"
      }
    },
    "numIntegrity": {
      "description": "num_integrity: Number of integrity values.",
      "type": "integer"
    },
    "integrity": {
      "description": "integrity: Measurement integrity.  Includes the integrity method used and an integrity value (which is to be interpreted based upon the integrity method).  The intent of allowing num_integrity > 1 is to report multiple integrity values based on multiple integrity methods.",
      "type": "array",
      "items": {
        "$ref": "type_integrity.json"
      }
    }
  },
  "required": [ "header", "timeOfValidity", "reference", "obs", "variance", "errorModel", "numErrorModelParams", "errorModelParams", "numIntegrity", "integrity" ]
}
